export function SiteFooter() {
  return (
    <footer className="border-t border-[var(--line)] py-6">
      <div className="mx-auto flex w-full max-w-6xl flex-wrap items-center justify-between gap-3 px-6 md:px-10">
        <p className="font-mono text-[11px] uppercase tracking-[0.2em] text-[var(--muted)]">
          PELANA TUJUH / 2026
        </p>
        <p className="text-sm text-[var(--muted)]">Creative media, live sessions, and cultural events.</p>
      </div>
    </footer>
  );
}
